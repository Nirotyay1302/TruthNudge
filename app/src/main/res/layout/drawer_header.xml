<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/logo_purple"
    android:padding="20dp"
    android:gravity="bottom"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"> <!-- Ensures text is light on dark background -->

    <!-- App Logo or User Profile Image -->
    <ImageView
        android:id="@+id/drawer_header_icon"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@drawable/truthnudge_logo"
        android:scaleType="fitCenter"
        android:contentDescription="@string/content_desc_app_logo"
         /> <!-- Optional: Tint if your logo is single color and needs to be white -->

    <!-- App Name or User Name -->
    <TextView
        android:id="@+id/drawer_header_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/app_name"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Headline6"
        android:textColor="@color/logo_purple_text_on" /> <!-- Ensures text is white/light -->

    <!-- User Email or App Subtitle -->
    <TextView
        android:id="@+id/drawer_header_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/drawer_header_default_subtitle"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
        android:textColor="@color/logo_purple_text_on_variant" /> <!-- SELF-CLOSING TAG FIXED -->

</LinearLayout>
